<a href=<%= project_path(locales[:project]) %> class="project p-4 bg-slate-100 hover:bg-slate-200 duration-300 rounded-md relative">
  <div class="rounded-full w-4 h-4 p-1 <%= locales[:pinned] ? 'bg-emerald-400' : 'bg-rose-400' %> absolute top-3 right-3"></div>
  <h1 class="text-2xl mb-2"><%= locales[:project].name.truncate(160)%></h1>
  <p class="text-sm">
    <%= locales[:project].description.truncate(160)%>
  </p>
</a>